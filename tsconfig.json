{
  "compilerOptions": {
    /* Base Options */
    "esModuleInterop": true, // Enables compatibility with CommonJS modules
    "skipLibCheck": true, // Skips type checking of declaration files
    "target": "ESNext", // Target latest ECMAScript features
    "verbatimModuleSyntax": true, // More precise module checks
    "allowJs": true, // Allow JavaScript files to be compiled
    "resolveJsonModule": true, // Allow importing JSON files
    "moduleDetection": "force", // Treat files as modules

    /* Strictness */
    "strict": true, // Enable all strict type-checking options
    "noUncheckedIndexedAccess": true, // More rigorous checks for array/object access
    "noImplicitAny": true, // Require explicit types
    "noImplicitOverride": true,

    /* Modules */
    "module": "ESNext", // Use ES module system
    "moduleResolution": "Bundler", // How modules get resolved (recommended for modern TS/bundlers)

    /* Path Aliases */
    "baseUrl": ".", // Base directory for resolving non-relative module names
    "paths": {
      "@/*": ["src/*"] // Map '@/' to 'src/'
    },

    /* Cloudflare Workers */
    "lib": ["ESNext"], // Include latest ES library features
    "types": [
      "@cloudflare/workers-types",
      "bun-types" // Adding bun types for test runner
    ], // Include Cloudflare Worker types globally

    /* Output */
    "outDir": "dist", // Output directory for compiled files (though wrangler often handles bundling)
    "noEmit": true, // Don't emit JS files (bundler like wrangler/bun will handle it)

    /* Source Maps */
    "sourceMap": true, // Generate source maps for debugging
    "inlineSources": true // Embed source code in source maps
  },
  "include": ["src/**/*", "tests/**/*"], // Which files to include in compilation
  "exclude": ["node_modules", "dist"] // Which files/directories to exclude
}
