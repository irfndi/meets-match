# TODO: Configure Cloudflare Worker Deployment
# This file is essential for deploying the TypeScript Grammy bot on Cloudflare Workers.
# Fill in the details based on your Cloudflare account and project requirements.
# Refer to Cloudflare Wrangler documentation: https://developers.cloudflare.com/workers/wrangler/configuration/

name = "meetsmatch-bot" # Replace with your desired worker name if different
main = "src/index.ts" # Entry point for the Worker
compatibility_date = "2024-04-01" # Use a recent compatibility date
compatibility_flags = [ "nodejs_compat" ]

account_id = "4d12ccdb69bc57519b6aac269a04c9af"

[[d1_databases]]
binding = "DB" # How you'll access it in your code (e.g., env.DB)
database_name = "prod-tele-meetsmatch-db" 
database_id = "0aa74e08-f123-460e-ba5e-3c34db8a9538" 

[[kv_namespaces]]
binding = "KV" # User-provided binding name
id = "prod-meetsmatch-KV" # User-provided KV namespace ID
# preview_id = "YOUR_PREVIEW_KV_NAMESPACE_ID" # Optional: Uncomment and set for 'wrangler dev'

[[r2_buckets]]
binding = "R2" # User-provided binding name
bucket_name = "prod-tele-meetsmatch" # User-provided R2 bucket name
# preview_bucket_name = "YOUR_PREVIEW_R2_BUCKET_NAME" # Optional: Uncomment and set for 'wrangler dev'

# TODO: Add build configuration if needed (e.g., for custom build steps)
# [build]
# command = "your_build_command_here"
# watch_dir = "src"

# TODO: Configure environments (e.g., staging, production) if necessary
# [env.production]
# name = "meetsmatch-bot-prod"
# account_id = "YOUR_PROD_ACCOUNT_ID"
# Routes, different bindings, etc.

# [env.staging]
# name = "meetsmatch-bot-staging"
# account_id = "YOUR_STAGING_ACCOUNT_ID"

[vars]
# Example: SOME_VARIABLE = "some_value"

[secrets]
BOT_TOKEN = "" # Placeholder, will be replaced by wrangler

# Optional: Define bindings to other Cloudflare services like KV, D1, R2
# [kv_namespaces]
# { binding = "MY_KV_NAMESPACE", id = "your_kv_namespace_id" }

# [d1_databases]
# { binding = "MY_DB", database_name = "my-database", database_id = "your_database_id" }

# Development server configuration (optional)
# [dev]
# port = 8787
# local_protocol = "http"
