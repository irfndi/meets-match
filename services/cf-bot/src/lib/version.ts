// Auto-generated by scripts/generate-version.ts
// Do not edit manually.

export const VERSION = "979562d7";
export const COMMIT = "979562d748949802001c0559a0a4cfb696be1acb";
export const BUILT_AT = "2026-05-14T19:34:11.559Z";
export const ENVIRONMENT = "development";
export const SERVICE_NAME = "cf-bot";

export interface VersionInfo {
  version: string;
  commit: string;
  builtAt: string;
  environment: string;
  service: string;
}

export function getVersionInfo(): VersionInfo {
  return {
    version: VERSION,
    commit: COMMIT,
    builtAt: BUILT_AT,
    environment: ENVIRONMENT,
    service: SERVICE_NAME,
  };
}

export function formatDuration(isoDate: string): string {
  const then = new Date(isoDate).getTime();
  const now = Date.now();
  const diff = Math.max(0, now - then);
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  if (days > 0) return `${days}d ${hours % 24}h ago`;
  if (hours > 0) return `${hours}h ${minutes % 60}m ago`;
  if (minutes > 0) return `${minutes}m ago`;
  return `${seconds}s ago`;
}
