{
  "name": "@meetsmatch/cf-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "npx tsx ../../scripts/generate-version.ts cf-worker && wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npx tsx ../../scripts/generate-version.ts cf-worker && tsc -p tsconfig.json",
    "test": "vitest",
    "lint": "npx tsx ../../scripts/generate-version.ts cf-worker && tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform": "^0.96.1",
    "@effect/sql": "^0.51.1",
    "@meetsmatch/cf-shared": "file:../../packages/cf-shared",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "wrangler": "^4.91.0"
  }
}
